resources:
  - deploy.yaml

configMapGenerator:
  - name: cache-conf
    files:
      - xrootd.cfg
      - scitokens.cfg
      - auth-file
      - voms-mapfile
      - stash-cache-renew-proxy.cron
      - renew-proxy
  - name: cache-supervisor
    files:
      - supervisord.conf

generatorOptions:
  disableNameSuffixHash: true

commonLabels:
  app: us-cms-datalake-cache
